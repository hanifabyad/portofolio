[build]
  command = "npm run build"
  publish = "dist"

[[plugins]]
  package = "@netlify/plugin-nextjs"
  [plugins.inputs]
    # This disables the Next.js plugin if it's being forced
    enabled = false
